import 'package:flutter/material.dart';

class AppDecoration {
  AppDecoration._();
  static double cardBorderWidth = 2;
  static BorderRadius get cardBorderRadius => BorderRadius.circular(10);
  static BorderRadius get pokeTypesBorderRadius => BorderRadius.circular(10);
}
